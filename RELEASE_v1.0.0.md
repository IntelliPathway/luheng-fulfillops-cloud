# 履约智控 AI · RepayGuard AI v1.0.0

`v1.0.0` 是面向受控试点的首个完整代码基线。它将资产接入、策略发布、活动预检、Agent 执行、保护事件、签约计划、回款对账和佣金账簿纳入同一条租户隔离、可审计的服务端链路。

## 本版本完成

- 策略变更采用服务端 Maker–Checker 工作流，包含版本检查、25 项确定性场景和证据摘要；
- 浏览器完成 OIDC Authorization Code + PKCE、state 验证、Token 刷新和登出；
- API 统一输出 `X-Request-ID`、结构化访问日志和请求计数、p50/p95 延迟指标；
- 电话 Provider 事件支持 HMAC 验签、5 分钟时间窗、幂等处理和租户级查询；
- 模型安全回放引入每租户每日费用预算与最坏情况预留；
- 试点评分卡聚合资产、运营、资金、待处理队列、阻断项和数据来源；
- 数据库补齐租户复合外键和业务检查约束；
- 资产导入契约升级为 `asset-case-v2`，增加本金、利息、费用、首逾期日和最近联系时间。

## 发布边界

- 真实外呼、支付和模型调用继续默认关闭；
- 生产 OIDC、电话/SIP、ASR/TTS、支付回调和模型凭据需由各环境负责人提供；
- `v0.19` 的 ECS/1Panel 生产部署未纳入本次开发链，不影响 v1.0 代码基线的完成；
- Sites 仅用于静态交互演示，不承载 FastAPI、Worker 或生产数据。

需外部资源或人工决策的事项统一记录在 [人工确认备忘录](docs/manual-confirmation-memo.md)。
