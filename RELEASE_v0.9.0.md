# 履衡 AI · FulfillOps Cloud v0.9.0

## 本版目标

将回款与佣金从浏览器演示数据升级为服务端权威资金证据链：只有通过 Provider 签名校验、幂等约束和案件匹配的回执才能进入回款账簿并触发应计佣金；确认结算和实际收佣继续作为独立事件登记。

## 已完成

- 新增支付 Provider 配置，签名密钥复用 `local-envelope` / AWS Secrets Manager 边界，数据库与 API 不返回明文。
- 正式 webhook 使用精确原始请求体做 `HMAC-SHA256 v1` 验签，并校验签名时间窗、CNY 币种和租户单笔上限。
- `(tenant, provider, provider_event_id)` 强制唯一：相同载荷返回原回执，不同载荷复用事件号返回冲突。
- 新增待复核回执队列；无法自动匹配的已验签事件不会进入钱指标，人工匹配要求显式确认并再次检查租户。
- 新增整数分回款账簿：记录支付/退款、计佣资格、规则版本、比例、原因、原事件和案件分配。
- 退款必须引用原支付，累计退款不得超过原金额，计佣资格和比例沿用原事件，避免按当前规则重算历史。
- 新增整数分佣金账簿：应计/冲回随回款原子生成，结算不得超过未结算应计，实收不得超过已结算未收。
- 钱指标快照改由账簿刷新；Agent ChatBI 同样读取账簿汇总，不再读取浏览器状态或独立样本数字。
- 回款与佣金页展示服务端回执状态、五项资金指标、证据字段及结算/实收事件；支付沙箱未就绪时操作明确禁用。
- 新增 PostgreSQL `008_financial_ledger.sql` 和 SQLite 兼容建表路径；既有演示回款迁移为带批次摘要的种子账簿事件。

## 验证口径

- 后端：63 项通过，1 项 PostgreSQL 专项按本地环境跳过；CI 使用 PostgreSQL 17 执行完整 64 项。
- 前端领域/API：15 项；Sites：4 项；发布基线共 83 项。
- `smoke-v09-financial-ledger.sh` 已验证 API `0.9.0`、¥1,016 回执只入账一次、坏签名 HTTP 401、越额实收 HTTP 409，以及最终回款/应计/结算/实收余额。
- 测试未连接真实支付、模型、语音或电话 Provider，不产生外部交易或费用。

## 部署边界

- 生产保持 `ENABLE_PAYMENT_SANDBOX=false`，按租户/Provider 从正式密钥后端注入 webhook secret。
- 正式支付 Provider 需在测试环境完成签名规范、事件重放、退款引用、乱序和并发回调验收。
- 当前账簿提供业务不可变追加语义；监管级总账、银行对账、会计凭证、税务和资金清算仍应由正式财务系统承担。
- PostgreSQL 路径已用租户账簿行锁串行化退款、结算和实收余额检查；正式财务系统接入时仍需验证其隔离级别、乱序补偿和日终对账策略。
