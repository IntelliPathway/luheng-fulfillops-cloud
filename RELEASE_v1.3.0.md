# 履约智控 AI · RepayGuard AI v1.3.0

## 目标

在不直接拨号的前提下，建立电话联系任务的合规预检、状态回传和人工接管闭环。

## 功能

- 只有电话 Provider 连接且集成总门禁启用后才可创建联系任务。
- 服务端检查案件保护、终态、联系依据、联系窗口和每日频次。
- 验签电话事件可推进任务状态；人工接管后 Provider 事件不再覆盖状态。
- 人工接管原因、操作人和时间进入审计链。

## 提效改造

- 使用 `(tenant_id, case_id, scheduled_at)` 索引支持频次门禁和案件轨迹查询。
- 频次校验在数据库中聚合计数，不加载历史任务明细。

## 边界

本版本不包含真实号码、SIP 账号或自动拨号；这些仍需人工确认备忘录中的 Provider 和合规批准。
