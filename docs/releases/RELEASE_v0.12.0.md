# 履衡 AI FulfillOps Cloud v0.12.0

## 本次迭代

- 新增租户隔离的签约方案、分期期次和回款分摊模型，以及 PostgreSQL 第 11 段增量迁移。
- 资产包增加最低结算比例、最大期数和最低首付比例；案件财务档案增加已核验债权余额。
- 运营人员提交方案与外部协议引用，只保存 SHA-256 摘要；协议正文不进入数据库或审计事件。
- 方案采用 Maker–Checker：提案人不能自审，管理员批准时重新校验版本、保护状态、当前资产包策略和委托期。
- 已验签回款按最早到期期次分摊；退款只引用原回款分摊并从最近期次逆向冲销。
- 方案完成或退款回退会同步维护履约方案与案件状态，Agent 和浏览器仍不能直接改写财务账簿。
- 前端新增「履约计划」工作台、待复核提醒、签约提案和独立审批弹窗；案件侧栏改为读取服务端期次与真实分摊。
- 新增 v0.12 HTTP 冒烟并纳入 GitHub Actions；ECS/1Panel 四容器部署边界保持不变。

## 安全与数据边界

- 所有金额以整数分存储，方案、期次、分摊和证据均按租户隔离。
- 保护案件、低于最低结算、首付不足、超期数、期次不连续、金额不平及委托期外签署均失败关闭。
- 方案编号对同一证据幂等，不同载荷复用编号返回冲突；同一案件不能并行存在多个待复核或生效方案。
- 审批采用乐观版本与数据库行锁，支付分摊复用现有验签、幂等和退款上限。
- Agent 只能查询服务端事实或生成结构化建议，不获得签约审批、支付验签或账簿写入能力。

## ECS 基线

- 服务器截图显示 `2 vCPU / 2 GiB / 40 GiB / 200 Mbps`。
- CPU 与带宽足够低流量验收，但内存和磁盘未达到最低门槛；当前结论仍为“不建议部署完整四容器栈”。
- 建议升级到至少 `2 vCPU / 4 GiB / 60 GiB SSD` 并配置 `2 GiB Swap`；推荐 `4 vCPU / 8 GiB / 100 GiB SSD`。
- 实际部署前仍需在 ECS 执行 `scripts/ecs-capacity-check.sh /opt/1panel`，核对可用内存、磁盘余量、Docker、Compose 与 1Panel 状态。

## 验证基线

- 后端：78 项，其中本地 77 项通过、1 项 PostgreSQL 专项按环境跳过。
- 前端/API 契约：20 项。
- ECS Compose 安全契约：1 项。
- Sites 构建与安全契约：5 项。
- 自动化测试总数：104 项；另有财务账簿、回执对账、保护事件和履约计划 HTTP 冒烟。
