# 安全策略

## 支持范围

安全修复只进入当前 `main` 和仍在维护的最新版本。历史原型与导出 HTML 不接收独立安全补丁；升级到最新版本后再验证。

## 漏洞报告

本仓库尚未配置公开漏洞邮箱。请通过仓库所有者的私有 GitHub Security Advisory 或组织内部安全渠道报告，不要在公开 Issue 中提交：

- 密钥、Token、支付签名或数据库内容；
- 可直接利用的认证绕过步骤；
- 客户、案件或真实个人数据；
- 未修复漏洞的公开复现链接。

报告应包含受影响版本、风险、最小复现、已知影响范围和建议缓解措施。不要对非自有环境执行扫描或利用。

## 信任边界

### 身份与租户

- 生产只接受企业 OIDC Bearer 身份；算法、issuer、audience、时间声明和 JWKS 必须验证。
- 工作空间由 `X-Tenant-ID` 指定，用户必须在数据库拥有有效成员关系。
- IdP 租户声明只能缩小权限，不能覆盖数据库成员角色。
- `X-Actor-ID` 和开发令牌仅允许开发环境；生产启动会拒绝相应开关。
- 提案人与复核人必须是不同账号，陈旧版本必须失败关闭。

### AI 与 Agent

- 模型不能直接修改账务、解除保护、发布策略或发起真实触达。
- Agent 只使用明确列出的业务工具；Shell、任意文件、任意 HTTP 不在授权范围。
- Runtime Grant 绑定租户、会话、范围和短时过期时间。
- 真实模型默认关闭，并受 HTTPS 白名单、模型白名单、Token、费用、租户密钥、自测和管理员确认约束。

### 支付与财务

- Webhook 使用精确原始请求体 HMAC-SHA256 v1、时间窗和单笔上限。
- Provider 事件在租户内幂等；冲突载荷拒绝。
- 未匹配或争议回执不进入业务指标。
- 退款引用原支付且不得超额；结算不得超过应计，实收不得超过已结未收。
- 浏览器和 Agent 无权直接写回款或佣金账簿。

### 密钥与数据

- 浏览器构建变量不得包含密钥。
- 服务端凭证使用外部引用、AES-256-GCM 信封或 AWS Secrets Manager。
- API 只返回掩码，不返回引用、密文或明文。
- 数据库不保存原始模型提示词/正文、原始支付请求体/签名或合同正文。
- 日志和审计只记录标准化错误、摘要和非敏感元数据。

## 生产启动门禁

`APP_ENV=production` 时，以下条件不满足会在数据库连接前拒绝启动：

- PostgreSQL `DATABASE_URL`；
- `AUTH_MODE=oidc`；
- 开发头身份和开发令牌关闭；
- 演示种子和 ORM 自动建表关闭；
- OIDC issuer、audience、无凭证 HTTPS JWKS URL 完整；
- CORS 为明确 HTTPS 来源；
- 独立 Runtime JWT 密钥至少 32 字符；
- `local-envelope` 使用非占位主密钥。

## 容器与网络

- 生产 API/Worker 使用 UID `10001` 非 root 运行；
- 根文件系统只读，只给 `/tmp` 和明确数据卷写权限；
- PostgreSQL 与 API 不映射公网端口；
- Web 只绑定宿主机回环地址，由 1Panel 终止 HTTPS；
- Nginx 设置 CSP、nosniff、frame、referrer 和浏览器权限限制；
- 真实模型出网只允许配置的主机，默认拒绝重定向、私网、本地地址和 URL 凭证。

## 上线前检查

- [ ] 目标 ECS 达到最低容量且通过只读检查；
- [ ] `.env` 没有 `CHANGE_ME`、示例密钥或开发域名；
- [ ] Git、镜像和构建日志中无凭证；
- [ ] OIDC 登录、退出、过期和跨租户拒绝已测试；
- [ ] 只开放 80/443，数据库和 API 内部端口不可公网访问；
- [ ] 真实模型、支付和通信 Provider 分别完成沙箱与生产审批；
- [ ] PostgreSQL 备份已加密、离机保存并完成恢复演练；
- [ ] 日志、指标、告警、值班和事件响应责任人已明确；
- [ ] 依赖、镜像和 SBOM 已在部署流水线扫描。

## 当前已知安全缺口

- 浏览器端通用 OIDC Authorization Code + PKCE 尚未内置，必须按最终 IdP 完成适配后才能对外生产使用。
- 自动化 SAST、依赖扫描、容器扫描和 SBOM 尚未进入当前 GitHub Actions。
- 完整审计留存周期、数据删除策略、RPO/RTO 和安全事件 SLA 尚需业务与合规共同确认。
- 数据库部分跨租户关联仍依赖应用层校验，需继续补复合外键约束。

这些缺口不得通过开启开发认证、暴露内部端口或把密钥写入前端来规避。
